name: cine_passe_app
description: Aplicativo de cinema que permite reservas de ingressos e gestão de planos de assinatura, com backend administrativo em React.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.8
  provider: ^6.1.1
  go_router: ^13.0.0
  font_awesome_flutter: ^10.6.0
  intl: ^0.20.2
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  firebase_core: ^4.2.1
  rxdart: ^0.28.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/icon.png"
  # adaptive_icon_background: "#FFFFFF" 
  # adaptive_icon_foreground: "assets/logo_foreground.png" 

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icon/
    - assets/